# mightycoderx.dev
Latest and greatest portfolio

## TODO
- [ ] Add extra keyboard row for mobile devices to allow use of Ctrl combinations and Tab
    Detect keyboard opening using the VirtualKeyboard API where available,
    or fallback to using the VisualViewport API to check if the height changes of about 300px
- [ ] Make a Command class and implement each command as a class inheriting it
- [ ] Add per-command tab completion by adding a method to the Command class
- [ ] Implement path traversal function using recursion
- [ ] Improve path completion using path traversal function
- [ ] Add pipes
- [ ] Allow to run multiple commands on single line (;, && and ||)
- [ ] Argument quoting
- [ ] Implement exit codes for commands
- [ ] Add grep command
